# Issues I Ran Into
1. prompt() returns a string. The prompt function always returns a user input as a string.
2. Function return values were not used. When I would call a function, I wouldn't store or display the return vale.